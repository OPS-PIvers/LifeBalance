# Cloud Functions params (firebase-tools loads this at deploy time).
# ADMIN_UID gates the global backfill callable (plan 06). Not a secret —
# the same UID is already public in firestore.rules isSuperAdmin().
ADMIN_UID=nmYdn3QPsNQEvniJEXW9M3lmV5e2
