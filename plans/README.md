# LifeBalance — Implementation Plans (index)

Generated by an audit + planning pass on **2026-06-21**, planned against commit `028cf23`.
Strategy and the full Claude-vs-human phasing live in **[`PRD.md`](./PRD.md)**. Raw audit
evidence (6 parallel auditors) is in [`audit/`](./audit/).

**Tags:** `[C]` Claude-executable end-to-end · `[H]` human-only (credentials/money/legal) ·
`[C→H]` Claude builds it, human flips one switch. **Every `[C]`/`[C→H]` ships via PR → merge to
`main` → CI auto-deploys** (`deploy.yml`); no personal login needed. See PRD §2.

> **Deploy risk rule:** any change to `firestore.rules` is high-blast-radius (atomic deploy, no
> staging) — it ships in its own PR, behind rules tests (Plan 010), and a human watches it. Code-
> only PRs are low-risk.

## Status table

| Plan | Title | Phase | Tag | Risk | Status |
|------|-------|-------|-----|------|--------|
| 001 | Server `processToggleHabit` sign fix (negative habits via iOS Shortcut) | 0 | C | LOW | **IN PROGRESS** |
| 002 | `calendarItemConverter` Timestamp→ISO guard | 0 | C | LOW | **IN PROGRESS** |
| 003 | Patch protobufjs RCE via `pnpm.overrides` | 0 | C | LOW | **IN PROGRESS** |
| 012 | Remove dead `sendtestnotification` guard + fix `isTimeToSend` minute-matching | 0 | C | LOW | **IN PROGRESS** |
| 004 | Content-Security-Policy + header hardening | 0 | C | MED | TODO |
| 006 | Rate limiter must fail **closed** (quickAdd) | 0 | C | MED | TODO |
| 005 | Wire "Download all my data" in Settings (B4) | 0 | C | LOW | TODO |
| 007 | `deleteHousehold` Cloud Function + confirm-to-delete UI (B3) | 0 | C→H | MED | TODO |
| 008 | Firebase Analytics instrumentation (measurementId already in env) | 0 | C | LOW | TODO |
| 009 | Remove hardcoded admin UID after claim provisioned (B2) | 0 | C→H | HIGH | TODO (blocked on Human-Checklist #3) |
| 010 | Firestore rules unit tests (`@firebase/rules-unit-testing`) | 1 | C | LOW | TODO |
| 011 | Privacy Policy + ToS draft + consent + AI notice (B5) | 0 | C→H | LOW | TODO |
| 013 | Allowlist → open-signup behind a feature flag | 0/1 | C→H | MED | TODO |
| 014 | Proxy Gemini through a Cloud Function (B1 real fix) | 0 | C→H | MED | TODO |
| 015 | **Investigate** money model: pending-txn double-count + voice `handleExpense` divergence | 0 | C | — | TODO (characterization tests first) |
| 020 | Onboarding wizard + starter-data seeding | 1 | C | MED | TODO |
| 021 | Empty-state gaps (Budget zero-data CTA; dashboard widgets) | 1 | C | LOW | TODO |
| 022 | Delightful partner-invite link (share/QR/deep-link) | 1 | C | LOW | TODO |
| 023 | `formatCurrency()` abstraction + `currency` field | 1 | C | MED | TODO |
| 030 | Playwright E2E skeleton (uses Test Mode) | 1 | C | LOW | TODO |
| 040 | Bound 3 unbounded listeners (`todo/14`) | 1 | C | MED | TODO |
| 050 | Stripe billing code (dormant, placeholder keys) | 2 | C | MED | TODO |
| 051 | Server-side entitlement checks | 2 | C | MED | TODO |
| 052 | Freemium gating (scale/convenience only) | 2 | C | MED | TODO |
| 060 | Weekly household recap (scheduled fn + push) | 3 | C | MED | TODO |
| 061 | Proactive AI insights (auto-surface) | 3 | C | MED | TODO |
| 062 | Referral / invite-reward system | 3 | C | MED | TODO |
| 063 | In-app notification badges | 3 | C | LOW | TODO |
| 064 | Fix hourly all-household scan (`todo/04`) + `sendbudgetalerts` N+1 | 3 | C | MED | TODO |
| 070 | Cost-control: STS summary doc, `todo/18`, `todo/19` | 4 | C | MED | TODO |
| 071 | Finish context split / shim migration | 4 | C | LOW | TODO |
| 072 | Admin panel + financial-edit audit log | 4 | C | MED | TODO |

Lower-severity audit findings not yet ticketed (atomicity MEDs CORRECTNESS-02/04/05/06, SEC-05/06/10/11,
PERF-04/05, DEPS-04/05/06, DX-03/05) are catalogued in [`audit/`](./audit/) and should be folded into the
nearest related plan as those files are touched.

## Execution order (this autonomous run)

Shipping the LOW-risk, high-confidence, code-only Phase-0 fixes first — **001, 002, 003, 012** —
on branch `claude/phase0-safe-fixes`, gated by `pnpm lint` + `pnpm test` + functions build, as one
reviewed PR. Then writing detailed plan files for 004–014 and the Phase-1 backlog. Risky (`009`
rules, `015` money-model) and `[C→H]` items are planned, not auto-merged.

## Findings considered and reframed / rejected

- **CORRECTNESS-03 "voice expense never debits checking"** — *not* a clean bug. `addTransaction`
  debits checking for every txn incl. `pending_review`, while Safe-to-Spend *also* subtracts
  `pending_review` (a latent double-count). The voice path's missing debit may be *more* consistent
  with the documented "manual balance" model. Reframed as **Plan 015 (investigate + characterization
  tests)**; do not auto-"fix".
- **SEC-08 / DEPS-01 protobufjs** — same root advisory, one fix (Plan 003). Not double-counted.
- **ARCH-03 = SEC-01 = DX-07** (admin UID in bundle) — one issue, handled with B2/Plan 009.
- **SEC-09 basic-ftp / DEPS-02** — dev-toolchain only (`firebase-tools`), not in the shipped app;
  low priority, fix opportunistically on a `firebase-tools` bump.
